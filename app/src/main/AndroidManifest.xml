<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Tournament Arena - phase 1A shell.
      No permissions are declared: the shell only navigates local mock data.
      Dark theme only; the theme itself disables force-dark.
    -->

    <application
        android:name=".TournamentArenaApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TournamentArena">

        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.TournamentArena.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.AuthActivity"
            android:exported="false"
            android:theme="@style/Theme.TournamentArena.Auth"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.TournamentArena.Main" />

        <activity
            android:name=".activities.TournamentDetailsActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity"
            android:theme="@style/Theme.TournamentArena.Main" />

        <activity
            android:name=".activities.NotificationsActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity"
            android:theme="@style/Theme.TournamentArena.Main" />
    </application>
</manifest>
